<?xml version="1.0" encoding="utf-8"?>
<resources>
    
    <!-- Badge Background for Secondary Badge -->
    <drawable name="badge_background">
        <shape android:shape="rectangle">
            <solid android:color="@color/secondary" />
            <corners android:radius="12dp" />
        </shape>
    </drawable>

    <!-- Badge Background for Primary Badge -->
    <drawable name="badge_primary_background">
        <shape android:shape="rectangle">
            <solid android:color="@color/primary" />
            <corners android:radius="12dp" />
        </shape>
    </drawable>

    <!-- Progress Bar Drawable -->
    <layer-list xmlns:android="http://schemas.android.com/apk/res/android">
        <item android:id="@android:id/background">
            <shape android:shape="rectangle">
                <solid android:color="@color/progress_background" />
                <corners android:radius="6dp" />
            </shape>
        </item>
        <item android:id="@android:id/progress">
            <clip>
                <shape android:shape="rectangle">
                    <solid android:color="@color/progress_primary" />
                    <corners android:radius="6dp" />
                </shape>
            </clip>
        </item>
    </layer-list>

    <!-- Circle for workout day indicator -->
    <drawable name="circle_primary">
        <shape android:shape="oval">
            <solid android:color="@color/primary" />
        </shape>
    </drawable>

    <!-- Arrow Left Icon (you'll need to add this as a vector drawable) -->
    <vector xmlns:android="http://schemas.android.com/apk/res/android"
        android:width="24dp"
        android:height="24dp"
        android:viewportWidth="24"
        android:viewportHeight="24"
        android:tint="?attr/colorOnSurface">
        <path
            android:fillColor="@android:color/white"
            android:pathData="M20,11H7.83l5.59,-5.59L12,4l-8,8 8,8 1.41,-1.41L7.83,13H20v-2z" />
    </vector>

    <!-- Button Ripple Effect -->
    <ripple xmlns:android="http://schemas.android.com/apk/res/android"
        android:color="@color/primary">
        <item android:id="@android:id/mask">
            <shape android:shape="rectangle">
                <solid android:color="@android:color/white" />
                <corners android:radius="10dp" />
            </shape>
        </item>
    </ripple>

    <!-- Card Shadow Selector -->
    <selector xmlns:android="http://schemas.android.com/apk/res/android">
        <item android:state_pressed="true">
            <shape android:shape="rectangle">
                <solid android:color="@color/card" />
                <corners android:radius="10dp" />
                <stroke android:width="1dp" android:color="@color/primary" />
            </shape>
        </item>
        <item>
            <shape android:shape="rectangle">
                <solid android:color="@color/card" />
                <corners android:radius="10dp" />
            </shape>
        </item>
    </selector>

</resources>